services:
  # Backend Service
  - type: web
    name: ai-sql-query-generator-b
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    rootDir: Backend_Part
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        fromService:
          type: web
          name: ai-sql-frontend
          property: url
      - key: FRONTEND_URL
        fromService:
          type: web
          name: ai-sql-frontend
          property: url
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_SECRET
        sync: false

  # Frontend Service
  - type: web
    name: ai-sql-frontend
    runtime: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    rootDir: Frontend_Part
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: ai-sql-query-generator-b
          property: url
        value: ${AI_SQL_QUERY_GENERATOR_B_URL}/api
      - key: VITE_GOOGLE_AUTH_URL
        fromService:
          type: web
          name: ai-sql-query-generator-b
          property: url
        value: ${AI_SQL_QUERY_GENERATOR_B_URL}/api/auth/google
